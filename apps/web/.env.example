VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Set to "true" to bypass real Supabase Auth and use a mock user (Juan Dela Cruz, Brgy. Captain).
# Useful for local development and E2E testing when the backend auth flow is not yet set up.
# NEVER enable in production.
VITE_ENABLE_MOCK_AUTH=false
